import React from 'react'

export const MarmsGitLearning: React.FC = () => {
  return (
    <div>
        <p>After creating the swamy branch and pushing the code to Git, we then started working on a new branch called marms</p>
        <p>Marms Git Learning:For Marms Git Learning, we created a new branch named marms and added a new component called MarmsGitLearning</p>
        <p>After that, we staged the changes, committed them, and pushed the code to the marms branch</p>
         <p>First, I will merge the latest changes from main into my marms branch.
                After resolving any conflicts, I will then merge the marms branch back into main</p>
    </div>
  )
}
